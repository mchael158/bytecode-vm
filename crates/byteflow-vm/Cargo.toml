[package]
name = "byteflow-vm"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Register-based bytecode interpreter for a single Byteflow virtual process"

[dependencies]
byteflow-bytecode = { path = "../byteflow-bytecode" }

[dev-dependencies]
