[package]
name = "byteflow-actors"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage = "https://github.com/mchael158/bytecode-vm"
documentation = "https://docs.rs/byteflow-actors"
description = "Embeddable flow runtime: M:N scheduler, Atomic Hop (Message+Cap), supervisor — use byteflow::"
readme = "README.md"
keywords = ["flows", "bytecode", "vm", "concurrency", "embedded"]
categories = ["concurrency", "emulators"]
include = [
    "src/**/*",
    "examples/**/*",
    "docs/**/*",
    "README.md",
    "CHANGELOG.md",
    "Cargo.toml",
    "LICENSE*",
]
# Library crate name is `byteflow` so dependents write `use byteflow::...`.
# Package name on crates.io is `byteflow-actors` (`byteflow` is taken).
[lib]
name = "byteflow"
path = "src/lib.rs"

[[bin]]
name = "byteflow"
path = "src/bin/byteflow.rs"
doc = false

[dependencies]
crossbeam-deque = "0.8"
num_cpus = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
# Clippy otherwise suggests converting `match` into unwrap_or / unwrap_or_default.
manual_unwrap_or = "allow"
manual_unwrap_or_default = "allow"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
